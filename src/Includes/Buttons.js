import React from 'react'

function Buttons(props) {
  return (
    <div>
     <button>{props.fullname}</button>
     <button>submit</button>
    </div>
  )
}

export default Buttons

// props concept in react